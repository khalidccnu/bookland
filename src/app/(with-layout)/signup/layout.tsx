import React from "react";

export const metadata = {
  title: "Sign Up | BookLand",
};

const Layout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default Layout;
